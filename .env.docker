# Docker Environment Configuration for Apexv6
# Copy this to .env when running in Docker

APP_NAME="Apex Attendance"
APP_ENV=production
APP_KEY=
APP_DEBUG=false
APP_URL=http://localhost:8080

# === LOCAL DATABASE (SQLite - for app data) ===
DB_CONNECTION=sqlite
# DB_DATABASE=/var/www/html/database/database.sqlite

# === SQL EXPRESS CONNECTION (for biometric data) ===
# SQL Express server IP address
SQLSRV_HOST=115.98.2.20
SQLSRV_PORT=1433
SQLSRV_DATABASE=etimetracklite1
SQLSRV_USERNAME=aboradiya
SQLSRV_PASSWORD=
SQLSRV_USERNAME=aboradiya
SQLSRV_PASSWORD=

# === IMPORTANT NOTES ===
# 1. SQL Express must allow TCP/IP connections (not just named pipes)
# 2. SQL Server Browser service must be running
# 3. Firewall must allow port 1433
# 4. Use SQL Server Configuration Manager to enable TCP/IP protocol

# === Session & Cache ===
SESSION_DRIVER=database
CACHE_STORE=database
QUEUE_CONNECTION=database

# === Mail (optional) ===
MAIL_MAILER=log
